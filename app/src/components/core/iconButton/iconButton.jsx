import React from 'react'

export default props => (
    <dev >
        <i className={'oi oi-cloud' + props.icon}></i>
        <span className="fi-briefcase"></span>
        <span className="oi oi-battery-full"></span>
    </dev>
)